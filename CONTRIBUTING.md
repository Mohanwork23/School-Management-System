# Contributing

Preferred workflow:

1. Create a feature branch from `main` (e.g., `feature/cleanup-config`).
2. Make changes and run tests locally: `mvn test`.
3. Push your branch and open a PR against `main`.

For security-sensitive changes (secrets, keys), do not store secrets in the repository — use environment variables or GitHub Secrets.
